#ifndef TRAVERSE_H
#define TRAVERSE_H

#include <stdio.h>
#include "Queue.h"
#include "node.h"

void single_traverse(Node *initial_node);
int multiple_traverse(Node **initial_nodes, int *array, int nums);

#endif
